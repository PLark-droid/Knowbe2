# Interactive Review Loop

Daniel's Review Loop (OpenAI Dev Day inspired) をベースにした反復レビューコマンド。

## 実行手順

### 1. 対象特定

変更されたファイルを特定:

```bash
git diff --name-only HEAD~1
```

または指定されたファイルパスを使用。

### 2. 6項目チェック

以下の6項目を順番にチェック:

| # | チェック項目 | ツール | 減点 |
|---|------------|--------|------|
| 1 | Type Safety | `npx tsc --noEmit` | -30/error |
| 2 | Code Quality | `npx eslint` | -20/error |
| 3 | Test Coverage | `npx vitest run --coverage` | -10 per % below 80 |
| 4 | Security | grep for known patterns | -40/critical |
| 5 | Performance | manual review | -10/issue |
| 6 | Code Style | consistency check | -5/issue |

### 3. スコアリング

- 基本点: **100点**
- 各項目の問題に応じて減点
- **合格ライン: 80点以上**

### 4. 反復ループ

スコアが80点未満の場合:

1. 問題点をリストアップ
2. 安全な修正を自動適用 (ESLint --fix のみ)
3. TypeScript・セキュリティの問題は手動修正を提案
4. 再チェック実行
5. **最大10回まで反復**

### 5. レポート出力

```
📊 Review Report
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Score: 92/100 ✅ PASSED
Iterations: 2

[1] Type Safety:    ✅ 0 errors
[2] Code Quality:   ✅ 0 errors (2 auto-fixed)
[3] Test Coverage:  ✅ 83.5% (threshold: 80%)
[4] Security:       ✅ No critical issues
[5] Performance:    ⚠️ 1 minor issue (-5)
[6] Code Style:     ⚠️ 1 inconsistency (-3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 実行方法

```
/review              # 直近のコミットをレビュー
/review src/         # 指定ディレクトリをレビュー
/review --auto-fix   # 自動修正を有効化
```

## 品質基準 (識学理論準拠)

- **結果重視**: スコア80点以上が絶対基準
- **曖昧性排除**: 6項目の明確な評価軸
- **責任明確化**: ReviewAgentが品質に対する全責任を負う
